// Package commons handles common non-domain features
//
// Some of these could be in `cmd/app` main package, but I suspect these will be reusable across apps, and so I would
// prefer to make these easily copy-pasteable, or be easy to create an actually public `commons` library.
package commons
